/**
 * REST network services. Add fetch/axios-based API calls here.
 */

// Placeholder: export REST API helpers when needed.
// e.g. export async function getConfig() { return fetch(...).then(r => r.json()); }
